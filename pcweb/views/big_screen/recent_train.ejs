<div class="swiper-container recomment-data-group" id="trainSwiper">
    <div class="swiper-wrapper">
        <% if(trainList.length > 0){ %>
        <% trainList.forEach(function (group) { %>
        <div class="swiper-slide recomment-data-container">
            <div class="row">
                <% group.forEach(function(item){%>
                <div class="col-4">
                    <div class="slide-item">
                        <div class="title">
                            <%=item.title.substring(0,10) %>
                        </div>
                        <img src="<%= item.pic %>" alt="培训图片">
                    </div>
                </div>
                <% })%>
            </div>
        </div>
        <%})}%>
    </div>
</div>
<div class="swiper-pagination" id="trainP"></div>