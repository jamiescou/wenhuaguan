<div class="swiper-container recomment-data-group" id="actSwiper">
    <div class="swiper-wrapper">
        <% if(actList.length > 0){ %>
        <% actList.forEach(function (group) { %>
        <div class="swiper-slide recomment-data-container">
            <div class="row">
                <% group.forEach(function(item){%>
                <div class="col-4">
                    <div class="slide-item">
                        <div class="title">
                            <%=item.name.substring(0,10) %>
                        </div>
                        <img src="<%= item.pic%>" alt="图片">
                    </div>
                </div>
                <% })%>
            </div>
        </div>
        <%})}%>
    </div>
</div>
<div class="swiper-pagination" id="activityP"></div>